#ifndef prog__flags
#define prog__flags

// Flags/Symbol = 1 : Present. Not Defined : Absent

// TRACK_MEMORY : only if symbol is defined
#define TRACK_MEMORY 1

// IS_IN_DEBUG : only if symbol is defined
#define IS_IN_DEBUG 1

// VM_PRINT_EACH_STEP : print each bytecode before executing it
#define VM_PRINT_EACH_STEP

#endif